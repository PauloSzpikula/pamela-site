export const social = {
	instagram: {
		handle: '@pamelateicefotografia',
		url: 'https://www.instagram.com/pamelateicefotografia',
	},
};

export const instagramHref = social.instagram.url;

/** Todos os perfis sociais, usado em sameAs de dados estruturados. */
export const sameAs: string[] = [social.instagram.url];
